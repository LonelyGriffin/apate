import {IApateConfig} from '../lib'

export const TEST_APATE_CONFIG: IApateConfig = {
  controlHost: 'localhost',
  controlPort: 8100,
  mockProtocol: 'http',
  mockHost: 'localhost',
  mockPort: 8000
}
