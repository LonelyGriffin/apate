import { ISerialized } from '../serializable';
import { IHttpResolver } from './http-resolver';
export declare const deserializeAnyResolver: (serialized: ISerialized<IHttpResolver>) => IHttpResolver;
